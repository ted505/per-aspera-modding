### SANDBOX MANIFEST
modId: Sandbox
initialSetup: InitialSetup.yaml
rule:
  filenames:
    - rule-patch.yaml
  replace: true
randomEvent:
  filenames:
    - random-event.yaml
  replace: false
popup:
  filenames:
    - popup.yaml
  replace: false